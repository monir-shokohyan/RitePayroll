import { Card, CardProps } from '@mantine/core'
import styled from 'styled-components'

export const SCard = styled(Card)<CardProps>`
  box-sizing: border-box;
  box-shadow: 2px 2px 8px #00000020;
`
